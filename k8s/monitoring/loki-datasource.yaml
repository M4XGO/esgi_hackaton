# INUTILE : le Helm chart loki-stack crée déjà un ConfigMap "loki-stack"
# avec le datasource Loki (label grafana_datasource: "1").
# Ce fichier est conservé comme référence mais ne doit PAS être appliqué
# pour éviter un doublon qui fait crasher le provisioning Grafana.
