apiVersion: apps/v1
kind: Deployment
metadata:
  name: itadaki-frontend
  namespace: services
  labels:
    app: itadaki-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: itadaki-frontend
  template:
    metadata:
      labels:
        app: itadaki-frontend
      annotations:
        k8s.v1.cni.cncf.io/networks: nad-services
    spec:
      containers:
        - name: frontend
          image: m4xgo/itadaki-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: NODE_ENV
              value: production
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 300m
